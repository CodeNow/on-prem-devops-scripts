# On-Prem Ansible Playbook

## Variables

### Variables Provided By Runnable

```
${HELLO_RUNNABLE_GITHUB_TOKEN}
${INTERCOM_ID}
${INTERCOM_KEY}
${SENDGRID_KEY}
${NPM_TOKEN}
```

### Variables Needed Before Installation From Installer

```
${AWS_ACCESS_KEY}
${AWS_REGION}
${AWS_SECRET_KEY}
${AWS_DOCK_SUBNET}
${DOMAIN}
${ENV}
${GITHBUB_APPLICATION_CLIENT_SECRET}
${GITHUB_APPLICATION_CLIENT_ID}
${GITHUB_TOKEN}
${MAIN_HOST_ACCESS_KEY}
${USER_CONTENT_DOMAIN}
```

### Variables Needed Before Installation That Can Be Randomly Generated

```
${MONGO_PASSWORD}
${MONGO_USERNAME}
${NAVI_SECRET}
${POSTGRES_PASSORD}
${POSTGRES_PASSWORD}
${RABBIT_PASSWORD}
${RABBIT_USERNAME}
```

### Variables Generated During Installation

```
${AWS_DOCK_SECURITY_GROUP}
${POSTGRES_HOST}
${VAULT_AUTH_TOKEN}
${VAULT_TOKEN_1}
${VAULT_TOKEN_2}
${VAULT_TOKEN_3}
```
